[build]
  base = "frontend-react"
  publish = "build"
  command = "npm install --legacy-peer-deps && CI=false npm run build"

# SPA fallback — all routes serve index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
  CI = "false"

# Optional: enforce HTTPS / security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
